.content
  .header
    %h1.page-title Items
    %ul.breadcrumb
      %li
        = link_to "Home", index_path
      %li
        = link_to "Items", items_path
      %li.active= @item.generated_name
  .main-content
    %ul.nav.nav-tabs
      %li.active
        %a{data: {toggle: "tab"}, href: "#view"} View
      %li
        %a{data: {toggle: "tab"}, href: "#edit"} Edit
    .row
      #myTabContent.tab-content
        #view.tab-pane.active.in
          %br
          = render partial: "form", locals: { no_edit: true, delete: true }

        #edit.tab-pane.fade
          %br
          = render partial: "form", locals: { no_edit: false, delete: false }
      -# .col-md-8
      -#   %br
      -#   .form-group
      -#     %label Attached Photos
      -#     - if @item.photo.attached?
      -#       %img{src: url_for(@item.photo), style: "width: 100%;"}
      -#       = form_for @item do
      -#         .form-group
      -#           = check_box_tag :delete_photo, class: "form-control"
      -#           %label Delete Photo

      -#         = submit_button("Confirm", "pull-left")
