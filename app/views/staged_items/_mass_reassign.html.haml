= submit_button("Reassign", classes = "submit-count mass-reassign", icon = "fa-arrow-right")
%table.table.table-striped.table-hover
  %thead
    %tr
      %th Name
      %th.hidden-xs ID
      %th.hidden-xs Quantity
      %th.hidden-xs Weight
      %th Expiry Date
      %th
  %tbody
    - @staged_items.each do |item|
      %tr
        %td
          %p= item.generated_name
        %td.hidden-xs
          %p= item.show_id ? item.id : ""
        %td.hidden-xs
          %p= item.quantity
        %td.hidden-xs
          %p= item.weight
        %td
          %p= item.expiry_date&.strftime("%Y-%m-%d")
        %td
          = check_box_tag "packed_item_ids[]", item.id, false, {id: item.id}
