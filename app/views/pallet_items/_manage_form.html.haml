- if @pallet.pallet_items.none?
  %h4.text-center No palleted items. Use the tabs to search or create a new item.
- else
  %h5 Update or remove existing items packed on this pallet.
  %button.btn.btn-primary{data: {target: "#mass-action-modal", toggle: "modal"}, disabled: true}
    %span.mass-action-count Mass Action
    %i.fa.fa-angle-double-right
  %br
  %br
  = render partial: "layouts/mass_action_modal", locals: {name: @pallet.name, redirect: pallet_pallet_items_path(@pallet), record: nil}
  = render partial: "layouts/list_quick_filter", locals: {placeholder: "Filter by name or ID...", assoc: "manage-items"}
  - @pallet.pallet_items.each do |pi|
    = form_for pi, url: pallet_pallet_item_path(@pallet, pi) do |f|
      = render "packed_items/manage_form_card", f: f, pi: pi, path: pallet_pallet_item_path(@pallet, pi)
