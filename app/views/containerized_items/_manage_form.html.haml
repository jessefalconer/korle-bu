- if @container.containerized_items.none?
  %p Use the tabs to search or create a new item.
- @container.containerized_items.each do |item|
  = form_for [@container, item], method: :patch do |f|
    .row.padding-top-small.inline-add-item
      .col-xs-9
        .row
          .col-xs-12
            .form-group
              %p.margin-bottom-0= item.generated_name
          .col-xs-7
            .form-group
              = f.text_field :expiry_date, value: item.expiry_date&.strftime("%Y-%m-%d"), class: "form-control datepickers", id: "containerized-item-#{item.id}"
          .col-xs-5
            .form-group
              = f.number_field :quantity, value: item.quantity, min: 0, class: "form-control"
      .col-xs-3.vertical-buttons
        = submit_button("Add")
        = delete_button(container_containerized_item_path(@container, item), "Are you sure?")
