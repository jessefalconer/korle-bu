- if @items.none?
  %h4.text-center No items to unpack.
- else
  %h5 Unpack and remove items. Items completely unpacked with no remaining quantity will not be shown here.
  = render partial: "layouts/add_hospital_modal", locals: {redirect: request.original_url}
  = render partial: "layouts/list_quick_filter", locals: {placeholder: "Filter by name...", assoc: "unpack-items"}
  - @items.with_inventory.each do |pi|
    - path = present(pi).unpack_path
    = form_for :unpacking_event, url: path do |f|
      = render "packed_items/unpack_form_card", f: f, pi: pi

  = paginate @items, window: 2
